# Kiwoom 0s 장운영구분 개발 기록

작성일: 2026-05-29

이 문서는 TradingDashboard에서 Kiwoom WebSocket 실시간 타입 `0s`(장시작시간/장운영구분)를 사용하면서 정리한 운영 규칙과 구현 판단을 남긴 기록이다. `0s`는 KRX/NXT 데이터 선택, 장마감 스냅샷, 장전/장중/장후 판단의 출발점이므로 앱 시작 시 가장 먼저 확인한다.

## 핵심 결론

- 앱 시작 후 조건식 조회보다 먼저 `0s`를 등록한다.
- `0s`에서 가장 중요한 값은 `215` 장운영구분이다.
- `214` 장시작예상잔여시간은 주로 KRX 기준 보조값으로 보고, NXT 판단의 주 기준으로 쓰지 않는다.
- `0s` 초기 수신이 항상 즉시 오는 것은 아니므로 짧은 타임아웃을 둔다.
- 타임아웃이면 상태 미확인으로 기록하고, 보수적으로 KRX 우선으로 시작한다.
- 이후 `0s` 이벤트가 들어오면 그 값으로 장상태와 NXT 사용 여부를 갱신한다.
- 휴장일, 서버 지연, 미수신 가능성이 있으므로 `0s` 단독 판단이 아니라 시간대와 실제 TR 응답으로 보완한다.

## WebSocket 등록 규격

로그인 후 아래 REG 패킷으로 등록한다.

```json
{
  "trnm": "REG",
  "grp_no": "1",
  "refresh": "1",
  "data": [
    {
      "item": [""],
      "type": ["0s"]
    }
  ]
}
```

응답은 `REAL` 메시지의 `values` 안에서 확인한다.

주요 필드:

| 필드 | 의미 | 사용 방침 |
| --- | --- | --- |
| `215` | 장운영구분 | KRX/NXT 및 장상태 판단의 주 기준 |
| `214` | 장시작예상잔여시간 | KRX 기준 보조값으로만 사용 |
| `20` | 체결시간 또는 이벤트 시각 계열 값 | 로그/검증용 보조값 |

## 215 장운영구분 사용 방침

`215`는 현재 장 상태를 나타내는 값이면서 상태 전환 이벤트로도 들어온다. 프로그램을 장중에 켜도 현재 상태가 한 번 내려올 가능성이 높지만, 항상 보장된다고 보지는 않는다.

운영 판단:

- NXT 장이 이미 열린 상태에서 프로그램을 켜면 `P` 같은 NXT 상태값이 내려올 수 있다.
- `P/Q/R/S/T/U/V` 계열은 NXT 프리/메인/애프터 관련 상태 판단의 중심으로 사용한다.
- 장중이면 시작 직후든 중간이든 해당 상태 코드가 유지되어 내려올 가능성이 높다고 본다.
- 휴장일에는 별도 휴장 전용 값이 명확히 오지 않거나 응답 자체가 지연될 수 있다.

## 214 잔여시간 취급

`214`는 장시작예상잔여시간이지만 NXT 프리/메인/애프터 각각의 정확한 잔여시간으로 신뢰하지 않는다.

운영 원칙:

- KRX 정규장 보조값으로만 사용한다.
- NXT 거래 가능 여부는 `215` 상태값과 실제 NXT 시세/TR 응답으로 판단한다.
- NXT 장전 시간대 거래 가능 여부는 `0s`만으로 명확하지 않을 수 있다.

## 앱 시작 순서

현재 권장 순서:

1. 앱 시작
2. `0s` 장운영구분 선조회 시작
3. WebSocket 로그인
4. `0s` 등록
5. 1~2초 정도 초기 `215` 수신 대기
6. 수신 성공 시 장상태 반영
7. 수신 실패 시 `장상태 미확인(KRX 우선)`으로 기록
8. 조건식 조회 시작
9. 조건식 종목 목록 구성
10. 종목별 NXT 가능 여부, 시장구분, 기준가 캐시 반영
11. 종목 선택 시 장상태와 종목 속성에 맞는 KRX/NXT 데이터 사용

실제 로그 예:

```text
[03:16:34] 앱 시작
[03:16:34] 0s 장운영구분 선조회 시작
[03:16:35] 0s 장운영구분 등록
[03:16:36] 0s 장운영구분 선조회 시간초과, 장상태 미확인(KRX 우선)
[03:16:36] 키움 조건식(01) 조회 시작
```

## KRX/NXT 데이터 선택 원칙

종목 선택 후 데이터별 기준을 분리한다.

- 기준가: 항상 KRX 전일종가를 기준으로 색상/등락 계산을 맞춘다.
- 호가: NXT 가능 종목이고 NXT 운영/마감 후 보존 시간대라면 NXT 호가를 우선 사용한다.
- 차트: NXT 가능 종목은 NXT 차트를 우선 표시한다. 특히 장마감 후부터 다음날 07:00 전까지는 NXT 표시 원칙을 유지한다.
- 프로그램매매: MTS와 동일 표시를 위해 `ka90008 + 종목코드_AL + 0봉 dt + prm_netprps_amt`를 사용한다.
- 매수/매도 체결량 누계: 장마감 후에는 신뢰 가능한 총 매수/매도 체결량 TR이 없어 공란 처리한다.

## 장마감 후 스냅샷

장마감 후에는 실시간 마지막 수신값이 비거나 오래된 값일 수 있으므로 REST 스냅샷을 사용한다.

KRX:

- KRX 정규장 종료 후 KRX 최종 스냅샷 사용
- KRX 종목은 KRX 스냅샷 유지

NXT:

- NXT 가능 종목은 NXT 20시 최종 스냅샷 사용
- NXT 스냅샷이 비어 있으면 기존 유효 데이터를 공백으로 덮지 않는다.
- 장마감 후 매수/매도 체결량은 공란 처리한다.

## 타임아웃과 보완 판단

`0s`는 반드시 즉시 온다고 가정하지 않는다.

방침:

- 초기 대기 시간 내 미수신이면 KRX 우선으로 시작한다.
- 이후 `0s`가 들어오면 NXT/KRX 모드를 재판단한다.
- 휴장일 또는 지연 수신 가능성은 로그에 남긴다.
- 실제 호가/차트/시세 TR 응답이 비어 있으면 기존 유효 데이터 보존 정책을 우선한다.

## 주의한 시행착오

- `0s`보다 조건식/종목 TR을 먼저 돌리면 NXT/KRX 판단이 늦어져 화면 데이터가 꼬일 수 있다.
- 장마감 후 KRX 호가를 먼저 표시한 뒤 빈 NXT 응답으로 덮으면 호가창이 공백이 되는 문제가 있었다.
- 빈 NXT 응답은 기존 호가를 지우지 않는 방어가 필요하다.
- `214`만 보고 NXT 시간대를 판단하면 위험하다.
- 프로그램 시작 시 `0s`가 타임아웃되더라도 나중에 들어오는 이벤트를 계속 반영해야 한다.

## 현재 구현 체크리스트

- [x] 앱 시작 시 `0s` 선조회
- [x] `0s` 초기 타임아웃 로그
- [x] 타임아웃 시 KRX 우선
- [x] 이후 `0s` 이벤트 수신 시 장상태 갱신
- [x] NXT 가능 종목 캐시 보유
- [x] 기준가는 KRX 전일종가 고정
- [x] NXT 가능 종목은 NXT 차트 우선
- [x] 장마감 후 매수/매도 체결량 공란 처리
- [x] 빈 호가가 유효 호가를 지우지 않도록 방어

## 향후 확인 질문

- `215` 코드 전체 표와 각 코드의 정확한 KRX/NXT 의미
- NXT 장전 시간대 거래 가능 상태가 `215`에서 별도로 구분되는지
- 휴장일에 `0s`가 내려주는 고정 상태값이 있는지
- `214`가 KRX 기준인지, 특정 NXT 상태에서도 의미가 있는지
- 앱을 장중에 실행했을 때 현재 상태 초기값 전송이 공식 보장인지

## 2026-06-03 가이드 확인 요청 메모

현재 구현 상황:

- 앱 시작 시 `0s`를 먼저 받고, 이후 실시간 `0s` 이벤트를 메모리 앵커로 사용한다.
- `0s`를 종목 선택 때마다 새로 조회하지 않고, 캐시된 `_lastMarketStatusCode`, `_lastMarketStatusAt`, 시간대 fallback으로 KRX/NXT를 판단한다.
- 조건검색/후보 캐시에 `GateBaseCandleMarket`, `SupportsNxt`, `CurrentPriceMarket`, `KrxDisplayPrice`, `NxtDisplayPrice`를 저장한다.
- 종목 선택 시 시장 근거는 `_selectedStockMarket`, `GateBaseCandleMarket`, 후보/최근조회 캐시, `SupportsNxt + NXT 시간대` 순서로 본다.
- 위 근거가 모두 없으면 현재 코드는 마지막 기본값으로 KRX를 사용한다.

현재 우리가 구분해서 쓰는 fallback 의미:

- KRX fallback 허용:
  - 시장 근거가 전혀 없는 종목의 기본 시장값.
  - 기준가/등락률/호가 색상 기준인 KRX 전일종가.
- KRX fallback 금지:
  - NXT 가능 종목을 NXT 화면으로 표시 중일 때 시가/고가/저가/현재가/호가/체결을 KRX 값으로 대신 채우는 것.
  - NXT 차트/분봉 조회가 비었을 때 KRX 차트/분봉으로 대체하는 것.

가이드에 물어볼 질문:

1. NXT 가능 종목에서 `GateBaseCandleMarket` 또는 명시 시장값이 없고, `0s`도 아직 미수신인 상태라면 화면 표시 시장을 임시 KRX로 두는 것이 맞는가?
2. 아니면 NXT 가능 종목은 시장 미확정 상태를 별도로 두고, KRX 표시로 fallback하지 않는 것이 맞는가?
3. NXT 가능 종목의 시세/호가/차트 TR이 빈 응답일 때, 화면 공백 방지를 위해 KRX 시세/호가/차트를 임시 표시해도 되는가?
4. 현재 판단은 "KRX fallback은 미확정 기본값이지, NXT 데이터 대체값이 아니다"인데, 이 문장이 키움 REST/SOR/NXT 표시 원칙과 맞는가?
5. `0s` 장운영구분이 장중 프로그램 재시작 시 현재 상태를 반드시 1회 내려주는지, 아니면 타임아웃 후 시간대 fallback을 유지해야 하는지 공식 확인이 필요한가?

### 2000자 이하 질문문

TradingDashboard에서 KRX/NXT 표시 시장 fallback 기준을 확인하고 싶습니다.

현재 구현은 앱 시작 시 `0s` 장운영구분을 먼저 받고, 이후 실시간 `0s`를 메모리 앵커로 사용합니다. 종목 선택 때마다 `0s`를 다시 조회하지는 않습니다.

종목의 표시 시장은 `_selectedStockMarket`, `GateBaseCandleMarket`, 후보/최근조회 캐시, `SupportsNxt + NXT 시간대` 순서로 판단합니다. 이 근거가 모두 없으면 현재는 임시 기본값으로 KRX를 사용합니다.

다만 NXT 가능 종목을 NXT 화면으로 표시 중일 때는 KRX 시가/고가/저가/현재가/호가/체결/차트로 대체하지 않고 있습니다. KRX에서 가져와도 되는 값은 기준가/등락률 기준인 KRX 전일종가뿐이라고 보고 있습니다.

확인 질문:

1. 시장 근거가 아직 없고 `0s`도 미수신이면 임시 표시 시장을 KRX로 두는 것이 맞습니까?
2. 아니면 NXT 가능 종목은 시장 미확정 상태를 별도로 두고 KRX fallback을 하지 않는 것이 맞습니까?
3. NXT 시세/호가/차트 TR이 빈 응답일 때 KRX 값으로 임시 표시해도 됩니까, 아니면 빈 값/대기 상태를 유지해야 합니까?
4. “KRX fallback은 미확정 기본값이지, NXT 데이터 대체값이 아니다”라는 원칙이 키움 REST/SOR/NXT 표시 기준과 맞습니까?
5. 장중 프로그램 재시작 시 `0s`가 현재 장상태를 반드시 1회 내려주는지, 아니면 타임아웃 후 시간대 fallback을 유지해야 하는지도 궁금합니다.

## 2026-06-03 가이드 답변 반영

확정 원칙:

- `0s` 미수신 상태에서 KRX를 임시 기본값으로 두는 것은 실용적으로 허용한다.
- 단, 이 KRX는 확정 시장이 아니라 `시장 미확정 임시값`이다. 로그 또는 UI에서 미확정 상태를 구분해야 한다.
- NXT 가능 종목을 NXT 화면으로 표시 중이면 KRX 시가/고가/저가/현재가/호가/체결/차트로 대체하지 않는다.
- NXT TR이 빈 응답이면 KRX 값으로 채우지 말고 빈 값/대기 상태를 유지한다.
- KRX에서 가져와도 되는 값은 기준가와 등락률 기준인 KRX 전일종가뿐이다.
- 장중 재시작 시 `0s`가 현재 상태를 내려줄 가능성은 높지만, 지연/미수신 가능성이 있으므로 타임아웃과 시간대 fallback은 유지한다.

개발 기준 문장:

```text
KRX fallback은 미확정 기본값이지, NXT 데이터 대체값이 아니다.
```

## 변경금지 규칙

이 항목은 2026-06-03 가이드 답변과 MTS 비교 기준으로 확정한 규칙이다.

수정금지:

- NXT 표시 경로에서 KRX 시가/고가/저가/현재가/호가/체결/차트를 fallback으로 채우는 변경.
- NXT TR 빈 응답을 KRX 값으로 덮어 빈 화면을 숨기는 변경.
- KRX 전일종가 외 값을 기준가/등락률 기준으로 사용하는 변경.
- `ResolveDisplayMarketForStockCode`의 마지막 KRX fallback을 NXT 대체 데이터 허용으로 해석하는 변경.

허용:

- 시장 근거가 아직 없는 종목의 임시 표시 시장을 KRX로 두는 것.
- 단, 이 경우는 `시장 미확정 임시값`이며 확정 KRX가 아니다.
- KRX 전일종가는 NXT 화면에서도 기준가와 등락률 색상 기준으로만 공유한다.
